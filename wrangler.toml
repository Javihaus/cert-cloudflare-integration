name = "cert-gateway-demo"
preview_urls = false
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
CERT_DASHBOARD_URL = "https://cert-framework.com"
CERT_PROJECT = "cloudflare-demo"

# Secrets — set via: wrangler secret put CERT_API_KEY
# CF_ACCOUNT_ID, CF_GATEWAY_ID, CERT_API_KEY are set as secrets, not vars
